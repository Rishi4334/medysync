rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Access to Firestore is mediated by backend Admin SDK.
    // Keep client-side access blocked by default.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}